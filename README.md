# QWZ Poker

Telegram Mini App for a poker room.

## Important

The bot token that was pasted into chat should be revoked in `@BotFather`.

Open `@BotFather`:

1. `/mybots`
2. Choose your bot in the row
3. `API Token`
4. `Revoke current token`
5. Put the new token into local `.env`

